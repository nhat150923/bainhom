#include<stdio.h>
 int main()
 {
 	int a;
  printf("nhap mot so a=");
  scanf("%d",&a);
if(a>0)
   printf("so do la so duong\n");
 else
 printf("so do la so am\n");
if(a%2==0)
   printf("so do la so chan\n");
 else
 printf("so do la so le\n");

}
